--!!type:strict
--!!mem:man

function main(): int
    local x: int = 42
    
    local p: ptr<int> = addr(x)
    
    local val: int = deref(p)
    print(val)
    
    store(p, 100)
    
    print(x)
    
    local size: int = sizeof(int)
    print(size)
    
    return 0
end
